- in trace.c next to gpgpu-sim.config the latency for dp can't be x,y where x < y.  
  i.e. latency !< initiation_interval
- We need to resolve the nan value in the correl in l2 write hit rate.
- Look errors in dram reads.
- l2 read hits error??????????
